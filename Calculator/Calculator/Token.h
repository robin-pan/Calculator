#include "stdafx.h"
#include <iostream>
#include <stack>
#include <string>
#include <sstream>

using namespace std;

string format(string input);

int count_tokens(string input);

string nth_token(string input, int n);